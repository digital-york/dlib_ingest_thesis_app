<% if @uploaded_file.new_record? %>
    alert('The file(s) could not be uploaded: <%= j @uploaded_file.errors.full_messages.join(', ').html_safe %>');
<% else %>
    $('#photos-list').prepend('<%= j render @uploaded_file %>');
<% end %>